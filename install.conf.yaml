- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vimrc: vimrc
    ~/.zimrc: zimrc
    ~/.zshrc: zshrc_zim
    ~/.zprofile: zprofile
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.zsh_plugins.txt: zsh_plugins.txt

- shell:
  - [git submodule update --init --recursive, Installing submodules]
